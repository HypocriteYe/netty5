syntax = "proto3"; // 版本
option java_outer_classname = "StudentPOJO"; // 生成的外部类名，同时也是文件名
// 使用，message管理数据，外部类生产的一个内部类，真正发送的POJO对象
message Student {
  // 属性的序号，不是值
  int32 id = 1;
  string name = 2;
}